[project]
name = "mocode"
version = "0.1.0"
description = "A CLI coding assistant powered by LLM"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "pyyaml>=6.0",
]

[project.scripts]
mocode = "mocode.main:main"
