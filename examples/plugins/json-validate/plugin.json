{
  "$schema": "https://agent-plugins.org/schemas/v1.json",
  "name": "json-validate",
  "version": "0.1.0",
  "description": "Validate JSON documents against a JSON Schema — a plugin with its own environment"
}
